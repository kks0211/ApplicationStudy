

Task (엑티비티와 같음)

- Stack  : 아래에서 위로 Task가 쌓임
  켜지는 방법
  -> launchMode
      //다중허용
      -standard
      -singleTop    ->    열려고 하는 액티비티라면 onNewIntent를 호출
      //다중허용안함
      -singleTask
      -singleInstance

  -> IntentFlag
    FLAG_ACTIVITY_NEW_TASK
    FLAG_ACTIVITY_SINGLE_TOP
    FLAG_ACTIVITY_CLEAR_TOP